"Please extract information regarding actions from the provided document and structure it in a standardized format as follows: Create a JSON object with two keys, 'actions' containing an array where each element is a string describing one action taken by individuals or groups within the text; include another key named 'entities', which should contain an array of objects. Each entity object must have three properties - 'name', representing who performed the actions (e.g., person names), 'action_performed' detailing what specific action they took, and 'time' indicating when this occurred if mentioned in the text. Ensure that all strings under 'actions' are concise sentences directly extracted from the document without paraphrasing or summarizing."

